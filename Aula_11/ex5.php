<?php

    // Cenário 5 – Biblioteca
    // Classe: Usuario → métodos: emprestar(Item), devolver(Item)
    // Classe: Aluno → métodos: — (herda Usuario)
    // Classe: Professor → métodos: — (herda Usuario)
    // Classe: Livro → métodos: —
    // Classe: Revista → métodos: —
    // Classe: Emprestimo → métodos: —
    // Classe: Biblioteca → métodos: registrarEmprestimo(Emprestimo), consultarDisponibilidade(Item)

?>
